name: oosmetrics Health Check

# TEMP: scheduled health check disabled — restore `schedule` trigger to re-enable.
on:
  workflow_dispatch:

# on:
#   schedule:
#     - cron: "0 9 * * 1"
#   workflow_dispatch:

permissions:
  issues: write

jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: oosmetrics/health-check-action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
