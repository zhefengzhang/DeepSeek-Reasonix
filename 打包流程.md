## Reasonix Windows 桌面程序编译指南

### 前置条件

| 依赖 | 说明 |
|---|---|
| **Node.js ≥ 22** | 项目运行时 |
| **Rust** | Tauri 后端编译需要。安装：`curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \| sh` |
| **Microsoft Visual Studio Build Tools** (Windows) | C++ 编译工具链，安装时勾选 "Desktop development with C++" |
| **WebView2** | Windows 10/11 自带；Windows 7/8 需手动安装 |

### 编译步骤

在项目根目录依次执行：

```bash
# 1. 安装依赖
npm install

# 2. 构建 CLI（生成 dist/ → tsup 打包 TypeScript）
npm run build

# 3. 下载 Windows 版 Node.js 到 desktop/src-tauri/binaries/node.exe
cd desktop
npm run bundle:node

# 4. 构建前端（React → dist）
npm run build

# 5. 编译 Tauri 桌面程序（Rust → MSI/NSIS 安装包）
npx tauri build
```

> **注意**: 第 5 步 `npx tauri build` 实际执行分为两步：先用 `npm run build`（已单独执行过），然后 Rust 编译器将 `desktop/src-tauri/` 编译为 Windows 原生可执行文件，最终输出 `.msi` 和 `.exe` 安装包。

### 关键配置文件

```
desktop/src-tauri/
├── tauri.conf.json          # 主配置（窗口尺寸、CSP、bundle）
├── tauri.windows.conf.json  # Windows 特有：bundle node.exe
├── Cargo.toml               # Rust 依赖 + release profile（LTO + strip）
├── binaries/node.exe        # 打包进安装程序的 Node 22 运行时
└── nsis/
    ├── English.nsh
    └── SimpChinese.nsh      # 中英文安装界面
```

### Release 编译优化

`Cargo.toml` 中 release profile 已配置：

```toml
[profile.release]
strip = true        # 去除调试符号，减小体积
lto = true          # 链接时优化
codegen-units = 1   # 更好的内联
panic = "abort"     # 更小的二进制
```

### Bundle 资源

安装包会自动包含以下资源（配置于 `tauri.conf.json` → `bundle.resources`）：

| 资源 | 路径 |
|---|---|
| CLI 入口 | `dist/cli/index.js`（根目录 `npm run build` 输出） |
| 分词器数据 | `data/deepseek-tokenizer.json.gz` |
| Node 运行时 | `node.exe`（`desktop/scripts/bundle-node.mjs` 下载） |

### 输出产物

`npx tauri build` 完成后，产物位于：

```
desktop/src-tauri/target/release/bundle/
├── msi/
│   └── Reasonix_0.52.0_x64_en-US.msi    # MSI 安装包
├── nsis/
│   └── Reasonix_0.52.0_x64-setup.exe     # NSIS 安装包
└── Reasonix.exe                           # 独立可执行文件
```

### 开发模式

```bash
# 在 desktop/ 目录下启动开发服务器（Vite HMR + Tauri WebView）
npm run tauri dev
```

这会同时启动 Vite 前端热更新和 Rust 后端的 Tauri WebView 窗口。

### 代码签名（可选）

如果发布正式版，建议对 Windows 安装包进行 Authenticode 签名以避免 SmartScreen 警告。详见 `desktop/SIGNING.md`，需要：

1. 从 CA（DigiCert / Sectigo）购买代码签名证书
2. 导出为 `.pfx`，base64 编码后设为 GitHub Secrets `WINDOWS_CERTIFICATE` + `WINDOWS_CERTIFICATE_PASSWORD`
3. CI 构建时自动签名

### 最小化快速编译命令

```bash
npm install && npm run build && cd desktop && npm install && npm run bundle:node && npm run build && npx tauri build
```